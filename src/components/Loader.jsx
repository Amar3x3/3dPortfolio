 import React from "react";
 import { OrbitControls, Points, PointMaterial, Text, Html } from "@react-three/drei";
 const Loader = () =>{
    return(
        <>
            <Html className="title intro center">Loading the Model</Html>
        </>
    )
 }
 export default Loader;